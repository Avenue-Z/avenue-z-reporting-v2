# Feedback Changelog

Terse one-line entries of what shipped. Lookup table to answer "did we already fix this?"

Format: `FB-NNN | <date> | <commit-sha> | <verification> | <one-line summary>`

Verification codes: `a` = reasoning only, `b` = ran dev server + clicked through, `c` = user-eyeballed.

---

## FB-041 — §H.2 Brand-Absent table (Content Impact)
- Collapsed §H.2 from 9 cols to 4: Domain, Article (title hyperlinked to URL), Citation Share (+Δ), Competitors Mentioned.
- Article column merges the old Article Title + URL cols using the §B Content Piece anchor pattern.
- Citation Share replaces Citation Count: `(c.citationCount / totalCitationsCurrentRows) * 100` with a pp delta, gated on compare period. Fixes a prior display bug that rendered raw counts with a `%` suffix.
- Sub-header swapped to Tina's verbatim copy. Dropped Prompt Cluster, Brand Mentioned, Opportunity Priority, Suggested PR Angle.
- Dead code dropped: `TT.opportunityPriority`, `TT.suggestedPRAngle`, `editorialDomains`, `filteredEditorialDomains`, six §H.2 demo arrays.
- §H.1 and the SectionCard wrapper untouched.

## FB-040 — §H.1 Competitor Analysis (Content Impact)

- Rebuilt §H.1 table to Tina's spec: Domain, AI Visibility (+Δ), Citation Share (+Δ), Prompt Coverage (+Δ).
- SectionCard title → `Competitor Analysis`. Subtitle + chart sub-header swapped to Tina's verbatim copy.
- Dropped Theme Coverage column; deleted dead `getThemeCoverage` helper, `TT.themeCoverage`, and the now-unused `domainTagIds` import.
- All deltas gate on compare-period (FB-034 hotfix #2 pattern). Zero new data fetches.
- §H.2 untouched.

## V2 Phase 2 - Content Impact UI fixes (2026-06-25)

FB-049 | 2026-06-25 | 89317f1 | a | §E slope chart refactored with right-margin legend (sorted by Current desc) + hover-to-mute behavior. Added useState(hoveredUrl). Layout split into flex row: chart left, legend ul right (w-56 shrink-0). Hovering a legend li or chart line activeDot sets hoveredUrl; opacityFor returns null=0.7/active=1.0+strokeWidth3/muted=0.15 for lines and 0.4 for legend rows. Tooltip only renders when hoveredUrl !== null and filters to the hovered URL inside its formatter. Pure helper lib/peec/slope-chart.ts not touched. Tina v2 CSV row 7.
FB-047 | 2026-06-25 | ea05535 | a | §D scatter Tooltip replaced with custom content function showing page path (bold header) + AI Bot Visits + Human Sessions on hover. Payload field names path/bots/humans match the existing scatter data. Tina v2 CSV row 6b.
FB-046 | 2026-06-25 | 631b2ee | a | §D scatter crosshair brightened from #FFFFFF40 (25% opacity) to #FFFFFF80 (50%) + strokeWidth 1.5 added. Corner-anchored absolute-positioned label overlay replaced with 2x2 CSS grid so each quadrant label sits in its true quadrant cell via flexbox anchoring. Labels: Low Bot High Human / High Bot High Human / Low Bot Low Human / High Bot Low Human. Tina v2 CSV row 6a.
FB-045 | 2026-06-25 | 84f16ec | a | §C Speed Stats Fastest + Slowest AI-Indexed tiles now render a hyperlinked source URL beneath the value (opens in new tab) so the day count can be verified against the actual page. Required threading url field through the urlTimings pipeline (computeUrlTiming didn't carry it; extended the input map to pull url from ContentCalendarRow). Tina v2 CSV row 5.
FB-044 | 2026-06-25 | 0ec1a41 | a | Delta columns split into own sortable columns across §B (9 cols -> 14), §F (6 cols -> 11), §H.1 (4 cols -> 7). Every metric column now has a matching Δ sibling with key '<metric>Delta', label 'Δ', accessor 'r.<metric>Delta ?? -Infinity' (so nulls sort to bottom on asc / top on desc), render via existing module-scope renderDelta helper. Units: pp for citationShare/promptCoverage/engagementRate/aiVisibility; pct for aiReferralTraffic/organicSessions. §H.1 Citation Share red bar visual preserved on the value column. §H.2 brand-absent table not touched per Tina's literal scope. Tina v2 CSV rows 4b + 8a + 9e.
FB-043 | 2026-06-25 | 0366d3f | a | §B Watched Pages footnote rewritten to name the 3 real causes of '--' (URL has not received visits yet, live URL differs from calendar entry, GA4 not configured to track that hostname). Added X of Y unmatched stat above the cause text (only renders when ga4Connected && unmatchedCount > 0). Extended PlannedContentPerformanceTable signature with unmatchedCount + totalPublishedCount props; orchestrator computes unmatchedCount as rows where all 3 GA4 fields are null. Tina v2 CSV row 4a.

## V2 Phase 1 - Content Impact metric coherence (2026-06-25)

FB-055 | 2026-06-25 | 3186eea | a | §H.2 Brand-Absent table Citation Share tooltip rewritten to describe share-of-period (was the stale TT.aiCitations text describing an avg count). Tina v2 CSV row 10. Math was correct since FB-041; only the tooltip was stale.
FB-054 | 2026-06-25 | a90175d | a | §H.1 Competitor Analysis Citation Share tooltip rewritten to describe share-of-period (was the stale TT.aiCitations text describing an avg count). Matches FB-051 math. Tina v2 CSV row 9d.
FB-053 | 2026-06-25 | 7e9fb23 | a | §H.1 column label renamed "AI Visibility" -> "Source Visibility" to match Peec's actual sourceVisibility metric definition (domain-level source presence, not brand-level visibility). Tooltip kept (was correct). Tina v2 CSV row 9c. Zero logic change.
FB-051-audit | 2026-06-25 | 2913872 | a | Full metric coherence audit covering all 22 metrics rendered on the Content Impact tab (§A KPIs, §B Watched Pages, §C Speed Stats, §F Fullsite Content, §H.1, §H.2). Doc at docs/official-feedback/content-impact-v2-metric-audit.md reconciles title + tooltip + value source + units against Peec metric-definitions.ts and GA4 semantics. 1 MISMATCH surfaced and fixed inline as FB-051a. Addresses Tina's V2 meta-feedback about misnamed / misrepresented metrics.
FB-051a | 2026-06-25 | 385af6a | a | §A KPI Citation Share + Prompt Coverage delta suffix corrected from % to pp via new optional KpiCard deltaMode prop. The deltas are percentage-point differences (current minus prior on already-percentage values), not relative percent changes. Found by FB-051-audit. AI Referral + Organic Traffic stay on the % default (true relative percent change). No breaking change to existing consumers.
FB-051 | 2026-06-25 | 5dbe453 | a | Killed the §H.1 Citation Share 199.9% bug. Root cause was multiplying Peec citation_rate (an avg count ~2.0) by 100. Added citationCount field to TopDomain populated from raw d.citation_count; §H.1 IIFE now uses share-of-period math (d.citationCount / totalCompetitorCitations) * 100 mirroring §B and §H.2. citationShareDelta stays truthful null (prior topDomains not plumbed, faking zero would lie). Synopsis context now sorts owned + competitor domains by real citationCount instead of citationRate, killing the silent bug where Glean was writing inflated AI citation claims. Removed the citationRate-as-citationCount remap hack. Cache version bumped v8 -> v9. Profound + demo-data updated for type parity. Tina v2 CSV row 9a + folded silent bug.
FB-042 | 2026-06-25 | afd9921 | b | §A Prompt Coverage delta wired via PR #85 (merged to main 2026-06-25 while this branch was being planned). Our redundant FB-042 commit was dropped during the rebase onto c493b30. Ask is satisfied in production. Tina v2 CSV row 3.

## FB-039 - Fullsite Content Performance (2026-06-25)

FB-039 | 2026-06-25 | <pending> | a | §F Fullsite Content Performance rebuilt per Tina ADD. Dropped the legacy 9-col domain-row OwnedContentCitedTable (1 row per owned domain, typically just avenuez.com). Built new 6-col URL-row FullsiteContentPerformanceTable: Page (hyperlinked title), Prompt Coverage, Citation Share, AI Referral Traffic, Organic Sessions, Engagement Rate. Each metric carries an inline comparison-period delta gated on compareIso !== null. Title and subtitle verbatim from Tina. Page label = UrlCitation.title, else labelFromPath(url), else raw URL. Row universe = urlCitations filtered to owned domains (urlJoinKey match against filteredOwnDomains) with citationCount > 0. Default sort Citation Share desc; pagination 10 with expand. Zero new fetches: per-URL prompt coverage from coverage.promptIdsByUrlKey (FB-035 Task 1), per-URL citation share from urlCitations + urlCitationsPrior (FB-035), per-path AI referral/organic/engagement from FB-035's aiReferredForPath/organicForPath/engagementRateForPath helpers. Lifted renderDelta helper to module scope in content-impact-tables.tsx so §B and §F reuse one copy. Dropped demoTopics/demoClusters/demoEngines/demoPositions/demoAiSessions arrays and enginesByDomain map from §F (all unused after the column shape change). Engagement rate value renderer + delta both multiply by 100 to mirror §B's existing handling (GA4 returns fraction [0,1]); correction shipped in hotfix commit 202073c. Universal across clients. tsc clean. All 6 test files pass.

## FB-038 - Slope chart (2026-06-25)

FB-038 | 2026-06-25 | <pending> | a | New §E ranked slope chart on Content Impact: "Which pages are gaining momentum and which are losing it?" per Tina's literal ADD. Verbatim title and subtitle. 3 toggle buttons (AI Referral Traffic / Organic Search Traffic / Citation Share) swap the Y-axis metric. Top 15 pages by absolute delta of the active metric (Tina's "biggest movers" language). Lines colored by direction: green gainer, red loser, gray flat, 70% stroke opacity. Position: between §D Bot vs Human Scatter and §F Fullsite Content Performance. Zero new fetches: all 6 source variables (3 metrics x 2 periods) reuse FB-035 Task 4 data already in scope (ga4AiPathRows / ga4AiPathPriorRows for AI referral, ga4ChannelMainRows / ga4ChannelPriorRows filtered to Organic Search for organic, urlCitations / urlCitationsPrior for citation share). Citation share per URL = urlCitationCount / periodTotalCitations * 100, period totals computed independently. Compare-period gated: renders only when compareIso !== null. Empty state otherwise: "Turn on a comparison period from the date picker to see which pages are gaining momentum across periods." (matches Tina's FB-034 "when you have a comparison period turned on" pattern). New pure helper lib/peec/slope-chart.ts with 7 test assertion blocks. New client component components/report-sections/peec-ai/slope-chart.tsx (useState toggle, Recharts LineChart with one Line per top-15 page, direction colors, empty states for compare-off and zero-points, citation-share Y-axis formatted as percentages). Universal across clients. tsc clean. All 6 test files pass.

## FB-037 - Bot vs Human scatter chart (2026-06-25)

FB-037 | 2026-06-25 | <pending> | a | New §D scatter chart on Content Impact: "AI Bot Traffic vs. Human Traffic" per Tina's literal ADD. Verbatim title and subtitle, 4 median-split quadrants. AI bot side from Peec /agent-analytics/visits per-path totals (agentData.byPath, already last-30 days from getAgentAnalytics). Human side from a new GA4 query (pagePath x sessionSource, hardcoded last-30 days to match the bot window, limit 1000) summed where !isAiSource (excludes ChatGPT/Claude/Perplexity/Gemini/etc.). Page universe is the union of paths with > 0 bot OR > 0 human traffic; (0,0) pages dropped. Boundary ties on the median go "low" (strict > comparison). Position: between §C Speed Stats and §F Fullsite Content Performance. New pure helper lib/peec/bot-vs-human-scatter.ts with 6 test assertions. New client component components/report-sections/peec-ai/bot-vs-human-scatter.tsx (Recharts ScatterChart, median ReferenceLines, 4 absolute corner labels, per-quadrant fill colors, tooltip, empty state). Subtitle explicitly says "Measures the last 30 days, independent of the page date range." to pre-empt the date-reactivity question (FB-036 pattern). Universal across clients. tsc clean. All 5 test files pass.

## FB-036 - Speed Stats validation (2026-06-25)

FB-036 | 2026-06-25 | <pending> | a | Tina asked two diagnostic questions about §C "How quickly does new content earn traffic and AI citations?" (a) real or hallucinated, has it been validated? and (b) affected by the date range selector or showing a default timeframe average like the YTD chart? Answered both with full receipts and shipped one copy fix caught while validating. Q1 answer: real GA4 data, validated. Source = Google's official BetaAnalyticsDataClient.runReport at lib/ga4/client.ts:12+185 (same SDK every GA4 metric in the platform uses); §C derivation is pure JavaScript math (min/max/median of firstSessionDate-publishDate per planned URL) via unit-tested helper computeUrlTiming in lib/ga4/content-derive.ts:133 with 3 assertions at content-derive.test.ts:69-97. Zero LLM in the path so nothing can hallucinate. Q2 answer: not affected by the date range selector by design. Query window hardcoded to earliestPublishDate->today at content-impact.tsx:580+592, same always-on pattern as the YTD Visibility chart. Intentional: "days to first" only makes sense from publish date forward through today; tying to the picker would clip out anything published before window start and exclude anything published after. Sub-finding shipped: §C subtitle previously said "first AI citation or bot crawl" but code only measures first AI-referred GA4 session (helper docstring confirms at content-derive.ts:128-131). Subtitle rewritten to accurately describe what's measured AND to call out the always-on behavior so the date-reactivity question doesn't recur. One-line edit at content-impact.tsx:1063. Behavior change zero. tsc clean.

## FB-035 - Watched Pages table overhaul (2026-06-24)

- Task 1: aggregateDomainCoverage now emits promptIdsByUrlKey; getDomainCoverage accepts dateRange opt; cache version v3 to v4 (coverage), v2 to v3 (citations).
- Task 2: SortableTable accepts defaultSortKey + defaultSortDir props.
- Task 3: Wired compareRange to ContentImpactReport at the dashboard route call site. Side-effect: FB-034 §A deltas now actually display when the user toggles comparison period.
- Task 4: 4 new GA4 queries (per-path channel-group current/prior, per-path full-metrics prior, per-path source prior) + 2 new Peec prior fetches.
- Tasks 5+6: §B table now renders 9 columns (Content Piece hyperlinked, Content Type, Publish Date, Last Updated, Prompt Coverage, Citation Share, AI Referral Traffic, Organic Sessions, Engagement Rate). Filtered to strict literal status === "published". Paginated at 10 with expand. Default sort Citation Share desc. New title and subtitle. Inline delta below each metric value when comparison period is on.

FB-035 hotfix #1 | 2026-06-24 | 0485065 | a | deriveCompareRange not parseDateRange. Preview at 46cd794 showed every comparison-period delta reading exactly 0.0% / 0.0 pp across both the §A KPI strip AND the new Watched Pages table. Root cause: content-impact.tsx:221 called parseDateRange(compareRange) directly. parseDateRange does not understand the magic strings 'previous_period' / 'previous_year' that the date picker emits, falls through to its default last-30-days branch, and silently returns the SAME window as the main range. Every prior fetch then returned identical data to its current twin, so every delta computed to 0. Bug originated in FB-034 Task 2 (10b88dc) and got amplified in FB-035 across 5 per-row deltas. Fix: import deriveCompareRange and swap to deriveCompareRange(mainRangeStr, compareRange) (the codebase pattern every other report already uses: ga4/index.tsx:91, inbound-funnel/index.tsx:78, pr-influence.tsx:205, demand-overview/index.tsx, peec-ai/index.tsx:310). Process lesson baked in: before adding data plumbing to a file, grep sibling components for the same symbol; if the file you are editing handles it differently than its peers, that is a question not a feature. New regression test lib/ga4/client.test.ts pins three behaviors (a) parseDateRange does NOT understand 'previous_period' / 'previous_year' (the canary) (b) deriveCompareRange shifts the window by its own width for 'previous_period' (c) deriveCompareRange shifts by exactly one calendar year for 'previous_year'. All assertions pass.
FB-034 hotfix #2 | 2026-06-24 | a4a3d12 | a | Citation Share delta gate. Peec returns prior values unconditionally (independent of compareRange), so Citation Share's delta was leaking even when comparison period was OFF. Now ALL 3 deltable KPIs (Citation Share, AI Referral Traffic, Organic Traffic) gate on compareIso !== null. Tina's literal "when you have a comparison period turned on" honored across the board. Prompt Coverage stays delta-less by design (no prior-period data).
FB-034 hotfix #1 | 2026-06-24 | b0c1afd | a | Removed validator Rule 2 (totalAiCitations false-positive on per-domain prose). Vercel logs on preview k5q7skomo showed the synopsis empty state firing consistently with `totalAiCitations mismatch: prose claims "3,196 AI citations" but context.totalAiCitations = 105239`. Glean was correctly writing per-domain prose like "example.com earned 3,196 AI citations" (3,196 is the per-domain count from topOwnedDomainsByCitations), but Rule 2's broad regex /N AI citations/ wrongly treated it as a total claim, threw twice, fell through to graceful empty state. Removed Rule 2 entirely; Rules 1 (brand-absent count) and 3 (owned domains cited) kept, both unambiguous. Test file updated: removed 2 Rule 2 tests, added regression test locking the per-domain false positive (prose "example.com earned 3,196 AI citations" with totalAiCitations=105239 must PASS). Cache bumped v2-glean-ci-kpi-swap → v3-glean-ci-rule2-removed to flush any retry-state cache. tsc + tests clean.
FB-034 revert | 2026-06-24 | 741dc69 | a | Reverted two overreaches that exceeded Tina's literal ask. (a) §A header restored from "Snapshot KPIs" back to original "How is content performing at a glance?". Tina's screenshot showed "Snapshot KPIs" as her own annotation label outside the card, not as a header change request. (b) Removed deriveCompareRange('previous_period') fallback so deltas render ONLY when the user explicitly turns on a comparison period via the date picker (Tina's literal language: "when you have a comparison period turned on"). When compareRange is not passed, no prior fetch happens and no delta line renders. Dropped now-unused deriveCompareRange import. tsc + tests clean.
FB-034 polish | 2026-06-24 | e2997d0 | a | Scrubbed 2 em-dashes that slipped into source-code comments during Task 5. The no-em-dash rule applies to all new code including comments. Functional change: zero.
FB-034 | 2026-06-24 | 989e5d1 + 10b88dc + 7845dbf + 16a117b + 3e9a940 | a | Content Impact §A Snapshot KPIs replaced with Tina's 4 new metrics + comparison-period delta display wired. Closes Tina v1 FEEDBACK ("Change these KPIs to: Citation Share, Prompt Coverage, AI Referral Traffic, Organic Traffic.") + flagged ISSUE ("Right now, when you have a comparison period turned on, it doesn't display change."). 5 commits. Task 1 (989e5d1) extends local KpiCard with delta?+invertDelta? props rendering arrow + magnitude + "vs previous period" matching Overview KpiCard exactly; when delta is undefined the line is omitted (no fake zero). Task 2 (10b88dc) accepts compareRange?: string on ContentImpactReport (page router was passing this; component was ignoring, root cause of Tina's ISSUE), derives mainIso + compareIso via parseDateRange + deriveCompareRange('previous_period' default), adds 2 GA4 queries to the existing Promise.allSettled with shape sessionSource × sessionDefaultChannelGroup × sessions for main + prior periods (single query shape feeds AI Referral Traffic and Organic Traffic for both periods, 4 KPI values from 2 fetches). Task 3 (7845dbf) computes 4 new KPI values + deltas: Citation Share = yourBrandCitations/totalCitations*100 with prior from peecData.yourBrandCitationsPrior + .totalCitationsPrior (delta in pp); Prompt Coverage = aggregate union-of-prompt-IDs-across-owned-domains/totalTrackedPrompts*100 (no prior in v1, getDomainCoverage has no dateRange arg, documented limitation); AI Referral Traffic = sum where isAiSource(sessionSource) with delta = ((cur-prior)/prior)*100; Organic Traffic = sum where sessionDefaultChannelGroup === 'Organic Search' with same delta math. Truth-grounded: prior unavailable → delta stays null → card omits delta line. Task 4 (16a117b) swaps §A JSX from 8 cards to 4 cards (Citation Share / Prompt Coverage / AI Referral Traffic / Organic Traffic) under "Snapshot KPIs" section header (Tina's section label from FB-032 deferred); demo-mode hardcodes 24.5%/67%/1,243/6,667 with deltas +2.3pp/--/+18.4%/-4.2%. Task 5 (3e9a940) refactors ContentImpactSynopsisContext: drops 6 orphaned old-KPI fields (plannedUrlsInScope, liveUrls, totalSessions, aiReferredSessions, ownedUrlsWithAiActivity, unmatchedPct), keeps totalAiCitations + ownedDomainsCited (validator Rules 2-3 still reference), adds new KPI fields. buildContext() Data section reorganized with 3 USE THESE EXACT VALUES section labels. Cache version v1-glean-ci → v2-glean-ci-kpi-swap flushes FB-033 cache. Validator rules 1-3 unchanged. Test fixture updated to new shape; all 10 assertions pass.
FB-033 | 2026-06-24 | fc66a93 + 054c718 + 30570d1 + 91e27f6 + b34b3bd | a | Content Impact: AI-generated Executive Synopsis card at top of tab per Tina's v1 ADD #1 ("AI-generated synopsis of overall performance & recommended actions during the period, executive overview style."). 5 implementation commits, FB-031 four-layer hardening pattern from day one. Task 1 (fc66a93) scaffolds lib/peec/content-impact-synopsis.ts with ContentImpactSynopsis + ContentImpactSynopsisContext types + Validator Rule 1 (brand-absent URL count); lib/peec/content-impact-synopsis.test.ts FIRST assertion reproduces FB-031 analog (context=5, prose=0, must flag). Task 2 (054c718) adds Validator Rule 2 (totalAiCitations, thousands-separator aware) + Rule 3 (ownedDomainsCited rounded-to-zero guard) + happy-path/no-zero-wording/empty-prose tests. Task 3 (30570d1) adds buildContext() with 4 `(USE THESE EXACT VALUES)` section labels + Glean prompt (executive tone + 1-decimal rule + no-em-dash rule + Data integrity strict rule) + extractJsonObject() three-tier extractor + retry-on-violation loop (max 2; second failure throws to graceful empty state) + cached() wrapper vendor='glean' fn='getContentImpactSynopsis' version='v1-glean-ci' ttlSeconds=3600 tagged by client+dateRange. Task 4 (91e27f6) builds components/report-sections/peec-ai/content-impact-synopsis.tsx RSC (Sparkles icon + Executive Synopsis eyebrow + paragraph prose + Recommended actions list + empty state "Synopsis is temporarily unavailable. Other metrics on this page are unaffected."); mirrors PR Influence synopsis shell verbatim. Task 5 (b34b3bd) mounts in content-impact.tsx: builds synopsisContext from same expressions §A KPI cards use (plannedUrlsInScope, liveUrls, totalSessions, totalAiCitations, aiReferredSessions, ownedUrlsWithAiActivity, unmatchedPct, ownedDomainsCited) so synopsis numbers cannot disagree with KPI strip; brand-absent count + top-3 brand-absent items mirror §H.2 live filter VERBATIM (`urlCitations.filter(c => !c.mentionsYourBrand && c.competitorBrandNames.length > 0)`) so synopsis count cannot disagree with §H.2 table; field-name correction made during implementation (plan assumed brandCitations/competitorCitations numeric fields, actual UrlCitation type uses mentionsYourBrand boolean + competitorBrandNames array; verified in lib/peec/url-citations.ts; logical derivation preserved). Insert Suspense+ContentImpactSynopsis between demo badge and §A label. Glean only (gleanChat from lib/glean.ts; no actAs). FB-025 carried forward: toLocaleString() for totals, toFixed(1) for per-row counts, prompt has Number formatting (strict) rule. Zero literal em-dashes in any new file. Renders in both demo and live modes (no calendarIsDemo gate). Type-check clean across all 5 commits. Test file: 1 regression assertion + 9 validator assertions, all pass.
FB-032 | 2026-06-24 | 46c00cc + 5ff00a3 + 923edea + d3fef39 + 5644c5d + 126dcc8 + d0f5e32 + 7e8514a + 7ff0e6b | a | Content Impact (v1) layout: 7-section deletion + dead-code cleanup per Tina's v1 recommended-layout doc. Layout-only round; no adds, no renames, no copy edits (those land next round). Sub-items: FB-032-a deletes §D "Which delivers more lift — new content or optimization?" + sectionDGroup/groupMedianFirstAi/newRows/optimizedRows compute + parseDateRange import (46c00cc). FB-032-b deletes §E "Which content is decaying vs. compounding over time?" + decayBuckets/decayOk + tallyTrajectories/Trajectory imports; lib/ga4/content-derive.ts kept (test consumer) (5ff00a3). FB-032-c deletes §G "Where is content disconnected from AI demand?" entire wrapper + all 3 sub-views (G.1 Traffic but No AI Citations, G.2 Citations but Little Human Traffic, G.3 Bot Attention but No Citations/Visits) + ownedHostSet/sessionsByPath/citedOwnedByPath/topicByPath compute + 3 G.* table imports + labelFromPath import (923edea). FB-032-d deletes §H.3 "Which competitor pages repeat across our target themes?" + urlTagNames import + RepeatedCompetitorPagesTable import; lib/peec/url-citations.ts kept (test consumer); §H wrapper preserved with H.1 + H.2 still rendering (d3fef39). FB-032-e deletes §I "Which AI systems are interacting with our content?" + bots alias + AISystemsInteractingTable import; filteredBots kept for §A KPI (5644c5d). FB-032-f deletes §J "What should the content team do next?" + 5 heuristic row builders + ContentTeamRecommendationsTable import (126dcc8). FB-032-g deletes bottom concatenated footer (FB-030 cross-tab pre-empt — same pattern Tina removed on PR Influence) + now-dead totalBotVisits compute (d0f5e32). FB-032-h code-quality follow-ups from review: deletes dead §E prior-period GA4 query (priorRange/priorRangeStr/ga4PriorResult slot/prior-period ga4Query/ga4PriorRows/rejected-logger) — kills a wasted GA4 round-trip per render; drops unused ContentCalendarData import; drops dead .action field from plannedTiming/urlTimings; scrubs 7 orphan comments; drops now-orphan deriveCompareRange import (7e8514a). Final cleanup commit drops 6 dead table component exports + Row types from content-impact-tables.tsx (TrafficNoCitationsTable, CitationsLittleTrafficTable, BotAttentionNoCitationsTable, RepeatedCompetitorPagesTable, AISystemsInteractingTable, ContentTeamRecommendationsTable) + PRIORITY_COLORS const + opportunityNote TT key + Globe2/Sparkles lucide imports; net -532 lines (7ff0e6b). Surviving content-impact-tables exports: PlannedContentPerformanceTable (§B), OwnedContentCitedTable (§F), CompetitorDomainsCitedTable (§H.1), CompetitorUrlsBrandAbsentTable (§H.2). Surviving page: SectionHeader → §A Snapshot KPIs → §B Watched Pages → §C Speed Stats → §F Fullsite Content Performance → §H wrapper with H.1 + H.2 (Competitor Analysis). All deletions in JSX itself, not behind calendarIsDemo gates — demo-on and demo-off render identically. Type-check clean. Library files (lib/ga4/content-derive.ts, lib/peec/url-citations.ts) intentionally untouched — their tests still consume the helpers; only the imports in content-impact.tsx were dropped. Deferred to next round: Tina ADDs (synopsis card, scatter chart, slope chart), Tina section labels (Snapshot KPIs/Watched Pages/Speed Stats/Fullsite Content Performance/Competitor Analysis), Tina ISSUE on Snapshot KPIs (comparison period delta wiring).
FB-031 | 2026-06-23 | 245905b | a | Hardened PR Influence Executive Synopsis against Glean contradiction (Vercel preview PR #64 showed synopsis claiming '0 brand-absent editorial domains' while Top Editorial Opportunities table on same page showed 5; both source from the same byHost map and cannot differ at render-time, so root cause was Glean hallucinating around a numeric count or stale cache slipping past arg-key). Four defensive layers: (1) prompt hardening — section labels marked 'USE THESE EXACT VALUES' + explicit 'Data integrity (strict)' rule forbidding rounding a positive count to zero or stating a positive number when count is zero; (2) new exported validateSynopsisGrounding in lib/peec/pr-influence-synopsis.ts scanning prose for three numeric-claim patterns (brand-absent host count, total editorial domains cited, N of M placements rate) and verifying each against synopsisContext; (3) retry-on-violation — one retry with a stricter prompt naming violations, then throw on second failure so component try/catch renders the graceful empty state; (4) cache version v2-glean-pri -> v3-glean-pri-grounded to evict pre-fix responses. New lib/peec/pr-influence-synopsis.test.ts with 13 assertions; first asserts the production bug is now flagged. Type signature unchanged; no render-layer changes; universal across clients. Overview synopsis (lib/peec/synopsis.ts) hardening reserved as follow-up.
FB-030 | 2026-06-23 | 364ee5c | a | Removed bottom footnote on PR Influence per Tina v1 CSV R24 ('REMOVE: This footnote at the very bottom of report. PR Influence on AI Visibility . Peec AI (live) . GA4 AI referral sessions (live) . N PR placements ...'). Deleted the trailing <p className='text-xs text-text-muted'> block in pr-influence.tsx. The matching string in the file's top-of-file code comment is preserved (not user-visible).
FB-029 | 2026-06-23 | 6d37b5a | a | PR Placement Matchback restored under Exec Summary per Tina v1 CSV R23 REVISION (was removed in FB-015 #81b2277 per V1 layout; V2 explicitly asks for it back). New focused 5-col PRPlacementMatchbackTable (Publication / Article / Publish Date / Cited by AI? / AI Engines) in pr-influence-tables.tsx. Tina's literal title 'Which secured PR placements are showing up in AI citations?' + literal subtitle verbatim. Rendered between <PRInfluenceSynopsis> and <SentimentInsights>. Reuses filteredMatchbackRows (date + model aware) and placementsCitedByAI. 'N of M placements cited by AI (rate%)' summary above table.
FB-028 | 2026-06-23 | 6cb2e25 | a | Top Editorial Opportunities rewritten per Tina V1 R15 ✅ + R16/R17 ⚠️. R15 ✅ 5-column shape preserved verbatim (Publication / Article / Competitors Mentioned / Citation Share / Delta of Citation Share). Row source flipped from data.topDomains (domain-rows) to urlCitations (URL-rows). Brand-absent now at URL level: c.mentionsYourBrand === false (R16 literal). Editorial filter: c.classification === 'editorial' (R16 Peec UI literal) with host cross-ref fallback. Citation Share = c.citationCount / sumOfAllPeriodCitations * 100. Delta of Citation Share = currentShare - priorShare from new prior-period URL fetch. Dropped legacy PR-placement-set misdef + positive-delta gate entirely. Cap raised from 20 to 50. URL fetches date-scoped to page range. Synopsis context sources from same byHost map for consistency.
FB-027 | 2026-06-23 | e9fbe2c | a | Prompt Clusters chart X-axis is now dynamic per Tina v1 CSV R14. domain={[0, upper]} where upper = next 5 (max ≤ 10) or next 10 (max > 10). Fallback to 5 when chartData is empty. One-block edit in PromptClusterOpportunityMatrix (components/report-sections/peec-ai/pr-influence-tables.tsx). No surrounding refactor.
FB-026 | 2026-06-23 | d385c55 | a | Sentiment Insights wired to live Glean-backed classification per Tina v1 CSV R4 + R5. New lib/peec/sentiment-insights.ts (Glean-backed, cached per clientSlug + dateRange + modelKey, 1h TTL) + lib/peec/sentiment-insights.test.ts (10 assertions, node:assert + tsx). sentiment-insights.tsx rewritten props-driven; SANDBOX_CLIENT_SLUG gate LIFTED (precedent: FB-023). Pill tint + label derive from live sentimentPct (Positive/Mixed/Negative thresholds). Empty state when zero analyzed URLs. pr-influence.tsx fetches getSentimentInsights server-side wrapped in try/catch; passes data via props. Universal across clients. Demo mode preserved.
FB-025 | 2026-06-23 | d20b321 | a | Synopsis decimals fix per Tina v1 CSV R2. buildContext (lib/peec/pr-influence-synopsis.ts) now interpolates d.citationCount.toFixed(1) and Math.round(o.score) instead of raw floats. Prompt strengthened with 'Number formatting (strict)' rule: at most 1 decimal in prose, integers stay integers, percentages render as N.N%, counts use thousands separators. Cache version v1-glean-pri -> v2-glean-pri to flush stale responses. No render changes.
FB-024 | 2026-06-23 | 47ea1cb | a | Pinned YTD visibility chart window to today (Jan 1 of current year through today's date) in both lib/peec/client.ts + lib/profound/client.ts — completely independent of the page date picker per Tina's literal "make static to always show YTD." Earlier FB-022 still derived end_date from the picker for custom historical ranges. Dropped the misleading "Tracking began" line from visibility-chart.tsx entirely (was deriving incorrectly from the chart's first data bucket; no source of truth available without a DB change Paul declined). Reverted FB-022's clients.firstTrackedAt DB column add + drizzle/0010_jittery_nextwave.sql migration + meta files + journal entry. Dropped firstTrackedAt prop threading from ProviderSection + VisibilityChart. Tooltip retained. No Neon touch.
FB-023 | 2026-06-23 | d9086ce | a | Winners/Losers cards swapped from static const arrays (FB-006) to live per-period per-model compute. Updated current promptBrandsRes (lib/peec/client.ts) with dimensions ['prompt_id','model_channel_id','model_id'] and limit 5000; added matching promptBrandsPriorRes. Built per-prompt-per-model position maps for both periods. Extended TrackedPrompt with positionByModel + priorPositionByModel (Partial<Record<AEOModel, number>>). New lib/peec/winners-losers.ts with applyModelFilter (collapses per-model maps to flat scalars restricted to selected models) + computeWinnersLosers (splits + sorts). 16 assertions in lib/peec/winners-losers.test.ts (node:assert + tsx, matching repo convention). winners-losers-cards.tsx rewritten props-driven; sandbox gate lifted; empty state mentions both date and model. peec-ai/index.tsx ProviderSection chains the two functions with active models prop. Profound TrackedPrompt mirrored for type parity (always-empty maps). Demo data backfilled with empty per-model maps. Tina v1 CSV E11. Reacts to date range AND model filter per literal text.
FB-022 | 2026-06-23 | 360d7c1 | a | Visibility trend chart now truly YTD. Added separate YTD fetch in lib/peec/client.ts (trendRowsYTD via fetchAllRows w/ ytd window) + lib/profound/client.ts (weeklyYTDRes via /v1/reports/visibility w/ ytd window). dailyVisibility/competitorDailyVisibility sourced from YTD; weeklyVisibility (demand-overview consumer) stays picker-range bound. Cache versions bumped (peec v7 to v8, profound v4 to v5). Added clients.firstTrackedAt column + Drizzle migration (migration NOT yet applied, Thomas to run after review). visibility-chart.tsx now sources tracking-start date from DB via prop; omits the line for clients without the value. Tooltip updated. Tina v1 CSV E7.
FB-021 | 2026-06-23 | ffbe3b5 | a | Removed "Which prompts are AI engines answering with our brand?" chart from both Peec + Profound Overview variants per Tina v2 + Rule #11. Deleted peec-ai/tracked-prompts-chart.tsx and profound-ai/tracked-prompts-chart.tsx. data.trackedPrompts field kept (consumed by 6+ other surfaces).
FB-020 | 2026-06-23 | 07f021e | a | Removed Overview SectionHeader subtitle per Tina v2 ask (CSV row E2). Made subtitle optional in SectionHeader; PR Influence / Content Impact / Technical Performance tabs still pass their own.
FB-001 | 2026-06-17 | 7097a19 | a | Unified AEO section header across all 4 tabs via shared SectionHeader component (Overview + PR Influence get the treatment, Content Impact unchanged, Technical Performance shifted yellow to green).
FB-002 | 2026-06-17 | ae8fc06 | a | AEO Overview tab redesign: removed what-changed pills (002a), swapped 3 KPI cards to Visibility / Citation Share / AI Referral Traffic with truth-grounded Citation Share math for both Peec and Profound (002b), added Vertex Gemini exec synopsis at top (002c — superseded by FB-003), added Snapshot KPIs eyebrow (002d), reordered trend chart below KPI grid (002e).
FB-003 | 2026-06-17 | e33ed66 | a | Migrated FB-002c synopsis from Vertex Gemini to Glean Chat API (project-wide Glean-only rule). Added gleanChat() helper to lib/glean.ts; surgical swap in lib/peec/synopsis.ts; cache version bumped to v2-glean; zero render-layer changes. lib/bigquery/gemini.ts (Fun Spot) still on Vertex, flagged as a future migration item.
FB-004 | 2026-06-18 | da74c23 | a | Added vertical axis to AEO Overview VisibilityChart. Five tick labels (integer % with % suffix) computed from existing CHART_MAX, aligned to the 5 existing gridlines. X-axis date row gets a matching w-9 spacer to preserve bar/label alignment. One file: components/report-sections/peec-ai/visibility-chart.tsx. Truth-grounded scale (no nice-rounding).
FB-005 | 2026-06-18 | 6142968 | b | Disambiguated "Google" in AEO Model Breakdown — discovered via live Peec API probe that Gemini data was being silently bucketed into the "Google" row (model_channel.id `google-2` = gemini-scraper, did not match "gemini" substring, fell into "google"). Fix: added model_id dimension to /reports/brands and /reports/domains queries; inverted normalizeSource read priority to prefer row.model.id (friendly scraper id like "gemini-scraper") over row.model_channel.id (channel id like "google-2"). Added MODEL_DISPLAY_LABELS map; "Google" canonical id now displays as "Google AI Overview". Verified end-to-end: before = 3 rows with merged Google bucket, after = 4 rows with separate Gemini + Google AI Overview. AI Mode add-on still lumps into Google bucket, flagged as future work.
FB-006 | 2026-06-18 | d9f8f70 | a | Added Biggest Winners and Biggest Losers cards to AEO Overview tab, side by side between Model Breakdown and Leaderboard. STATIC content: Tina's verbatim 17 winners + 20 losers from her AEO Analysis doc (2026-06-16, Profound, Last 14 Days, ChatGPT) live inline in the component as two const arrays. No props, no data layer, no fetch. Symmetrical layout via grid lg:grid-cols-2 + items-stretch; each card body capped at max-h-[400px] overflow-y-auto so the page footprint stays bounded. Initial commit `364f696` was a computed/live version; superseded by `d9f8f70` per Thomas: static for now. Swap to live data later by replacing the two const arrays with props.
FB-007 | 2026-06-18 | 2077037 | a | Removed BrandSOVChart ("Which categories of brands earn AI share of voice?") and BrandDefinitions ("What do these brand categories mean?") from the AEO Overview tab per Tina. Collapsed the surrounding lg:grid-cols-[1fr_280px] wrapper so the Rankings table ("Which brands appear most often in AI answers?") stretches full width. Also deleted the unused BRAND_TYPE_MAP/BRAND_TYPE_COLORS/BRAND_TYPE_DEFINITIONS import. Domain Types section directly below is untouched. One file: components/report-sections/peec-ai/index.tsx.
FB-008 | 2026-06-18 | c19733e | a | Recolored the AEO Overview Domain Types chart ("What kinds of sources do AI models cite?") and matching legend ("What do these domain types mean?") with the Avenue Z brand palette per Tina. Extracted shared DOMAIN_TYPE_COLORS const so chart bar and legend dot always match. Primary categories use all five brand accents at 100%: Own cyan, UGC green, Editorial blue, Corporate yellow, Competitor purple. Secondary categories use parent-accent at 60% opacity (Reference = blue 60%, Institutional = purple 60%). Other uses white 20%. Zero #8A8A8A gray remains in the file. Brand-doc compliant (opacity on accents is the same technique the brand doc uses for focus/borders).
FB-011 | 2026-06-22 | b4906a2 | a | Placement correction for FB-010. Moved <SentimentInsights /> from between Section B (PR Placement Matchback) and Section C (Top Editorial Domains) to between the Executive Synopsis and Section B. Strict re-read of Tina's mockup: her sequence is Synopsis -> Sentiment Insights -> Top Editorial Domains; Matchback wasn't on her sketch so it lives below. One JSX move + inline comment update in components/report-sections/peec-ai/pr-influence.tsx. New page order: SectionHeader -> Synopsis -> Sentiment Insights -> Matchback -> Top Editorial -> Brand-Absent -> Opportunity Matrix -> Next Pitch -> Methodology. Component itself unchanged; same Avenue Z sandbox gate.
FB-019 | 2026-06-22 | ff14652 | a | Post-merge layout polish. Thomas: "match these perfectly so theres no dead space between them and also make the which prompt clusters table look less anemic." Side-by-side wrapper is `grid lg:grid-cols-2` (default align-items: stretch); Prompt Clusters chart at 414px was forcing the Top Editorial Domains card to stretch ~100px past its natural content, leaving visible dead space. Fix: tightened chartHeight from `Math.max(220, len*34+40)` to `Math.max(200, len*24+36)` (~25% shorter), reduced `barCategoryGap` 8 -> 4, added explicit `barSize={14}` so bars stay prominent at the tighter spacing. Both cards now land ~470px and end flush; bars are visually thicker. Files: components/report-sections/peec-ai/pr-influence-tables.tsx.
FB-018 | 2026-06-22 | 30465fd | a | Copy correction on FB-010 Sentiment Insights. Tina wrote "When you click on a theme..." but FB-010 shipped "Tap a theme to see..." Replace_all from "Tap a theme" -> "Click a theme" across both column-intro paragraphs. One-word change, zero behavior change. Files: components/report-sections/peec-ai/sentiment-insights.tsx.
FB-017 | 2026-06-22 | 75db637 | a | Label correction on FB-010 Sentiment Insights. Tina's literal layout spec said "Positive Themes & Negative Themes side-by-side" but FB-010 shipped the right column as "Weaknesses" because Tina's underlying CONTENT for that column was framed that way (Unclear Answer Engine Methodology, Unproven Answer Engine Impact — gaps). Renamed to Tina's literal text. Changes: <h4>Weaknesses</h4> → <h4>Negative Themes</h4>; "Tap a weakness to see the explanation." → "Tap a theme to see the explanation."; updated the top-of-file comment. Zero changes to the data array, state, behavior, or Avenue Z sandbox gate. Files: components/report-sections/peec-ai/sentiment-insights.tsx.
FB-016 | 2026-06-22 | 4215718 | a | Visual bug fix on the FB-012 Prompt Clusters bar chart. Tina spotted "the percentage is, like, in black, and you can't see it." Recharts <Tooltip> has 3 separate style props (contentStyle for container, labelStyle for the row label, itemStyle for each data row). I had set color:'#FFFFFF' on contentStyle only, but Recharts' DefaultTooltipContent applies its own near-black color:rgb(51,51,51) default to the item row, which is invisible against the dark #272727 tooltip background. Fix: added labelStyle={{color:'#FFFFFF', fontWeight:600}} + itemStyle={{color:'#FFFFFF'}} to the <RechartsTooltip>, removed redundant contentStyle.color. Tooltip now renders cluster name in bold white + metric line in white. Files: components/report-sections/peec-ai/pr-influence-tables.tsx.
FB-015 | 2026-06-22 | 81b2277 | a | Close FB-014 open risk #1. Remove PR Placement Matchback from the AEO PR Influence tab — Tina's recommended-layout mockups across FB-011, FB-012, and FB-014 (three rounds) consistently omit Matchback, and Thomas's stated layout order (synopsis → sentiment → top editorial → prompt clusters → top editorial opportunities) is 5 sections, no Matchback. Removed: PRPlacementMatchbackTable JSX render in pr-influence.tsx; matchbackTableRows builder block; 4 demo arrays that fed it (DEMO_PROMPT_CLUSTERS, DEMO_AI_ENGINES, DEMO_PROMPT_COUNT, DEMO_POST_PUBLISH_TREND); PRPlacementMatchbackTable + PRPlacementMatchbackRow + AI_ENGINES_TOOLTIP + POST_PUBLISH_TOOLTIP from pr-influence-tables.tsx; PRPlacementMatchbackTable + PRPlacementMatchbackRow imports. KEPT: buildMatchback helper + matchbackRows + filteredMatchbackRows + placementsCitedByAI compute — the executive synopsis context still uses placementsCitedByAI for "X of Y placements cited by AI" prose. Final tab render order: SectionHeader → PRInfluenceSynopsis → SentimentInsights → [TopEditorialDomainsTable + PromptClusterOpportunityMatrix side-by-side in grid lg:grid-cols-2] → BrandAbsentEditorialDomainsTable (Top Editorial Opportunities) → footer. 5 visible sections, structurally 1:1 with Tina's mockup. Universal change. Files: components/report-sections/peec-ai/pr-influence-tables.tsx, components/report-sections/peec-ai/pr-influence.tsx.
FB-014 | 2026-06-22 | 293599c | a | Per Tina's Jun 17 doc batch on the Brand-Absent card + the Next Pitch section. RETITLE the Brand-Absent table as "Top Editorial Opportunities" (new title: "What are the top pitch opportunities for getting our brand mentioned in AI?"; new subtitle: "Prompt-level citations on the rise where your brand is not mentioned, revealing outreach opportunities that may require different strategies depending on the type of article being cited."). COLUMNS revised to 5: Publication, Article (combined title + URL as a hyperlinked cell), Competitors Mentioned, Citation Share, Delta of Citation Share. Removed columns: Brand Mentioned, Opportunity Priority, Suggested PR Angle. FILTER: brand-not-mentioned (or no data, for manual check) AND positive delta on citation share (`d.retrievedDelta > 0`); applied at table-row build step only — synopsis context keeps the unfiltered brand-absent count for executive accuracy. Removed: bottom footnote, `isDemo` prop, BrandAbsentEditorialDomainRow fields (brandMentioned/opportunityPriority/suggestedAngle), added `citationCountDelta` field. DELETED: entire NextPitchOpportunitiesTable component + NextPitchOpportunityRow type from pr-influence-tables.tsx; nextPitchRows + nextPitchEmptyKind compute + Sparkles JSX wrapper from pr-influence.tsx; Sparkles import. (Tina's "Where should we pitch next to close AI visibility gaps?" REMOVE.) The "How is the opportunity score calculated?" REMOVE was already shipped in FB-012. Matchback table KEPT in place — Tina did not include it in her REMOVE list; carrying as the highest-likelihood next-feedback item. Universal change (no Avenue Z sandbox gate needed). Files: components/report-sections/peec-ai/pr-influence-tables.tsx, components/report-sections/peec-ai/pr-influence.tsx. Diff: -254 / +53 net.
FB-013 | 2026-06-22 | 41d2091 | a | Fix pre-existing data bug surfaced by FB-012's bar chart. computeOpportunityRows() was computing editorialCitationDensity ONCE globally (totalEditorialCitations / N) and assigning the same value to every cluster row, so the FB-012 chart rendered identical 100% bars across all clusters with no ranking. Old 7-col table hid this because 6 other columns varied per cluster (Tina's original screenshot also shows Editorial Citation Density at 100.0% for every row). Fix: compute editorialShare PER CLUSTER using data already fetched — coverage.tagNameById (reversed to clusterName -> tagId), coverage.tagIdsByDomain (host -> tag ids), topDomain.retrieved (citation share %). For each cluster: editorialShare = sum(retrieved across editorial-typed domains tagged with cluster) / sum(retrieved across ALL domains tagged with cluster) * 100. computeOpportunityRows() signature now takes (trackedPrompts, editorialDomains, topDomains, coverage). Call site at pr-influence.tsx:362 updated. No render-layer changes, no fetch changes, no demo-mode changes. opportunityScore values shift downstream (affects Next Pitch priority badges only — the score itself is no longer rendered to users post-FB-012). Files: components/report-sections/peec-ai/pr-influence.tsx.
FB-012 | 2026-06-22 | f1d5c5a | a | Reduce + redesign on the AEO PR Influence tab per Tina's Jun 17 batch (+ Whitney "agreed"). TOP EDITORIAL DOMAINS: rename Citation Count -> Citation Share; drop Avg Citations + PR columns; drop bottom-left blue/green legend; swap subtitle to "These domains are the most likely to surface as cited sources in AI-generated results, so they should be prioritized on the media target list."; drop green-on-PR styling (Domain text + bar) since the legend that explained it is gone; remove unused isDemo/prDataAvailable props. PROMPT CLUSTER OPPORTUNITY: entire 7-column SortableTable replaced with a Recharts horizontal BarChart over editorialCitationDensity, descending, fixed 0-100% domain, editorial-blue single color. New subtitle "Topics ranked by share of citations earned from editorial sources. Higher share means a stronger candidate for the next PR pitch." Old "Opportunity Score = 35% editorial..." footer line removed. LAYOUT: both reduced cards now sit side-by-side in a grid gap-6 lg:grid-cols-2 wrapper directly under SentimentInsights; PR Placement Matchback moves below them (matches Tina's "Recommended layout" screenshot and the FB-011 open risk that anticipated this exact reorder). Removed the bottom "How is the opportunity score calculated?" 4-weight methodology block from pr-influence.tsx — it explained an opportunityScore number that no longer renders anywhere visible. Universal change (no Avenue Z sandbox gate needed — pure design / layout / UX). Files: components/report-sections/peec-ai/pr-influence-tables.tsx, components/report-sections/peec-ai/pr-influence.tsx. Diff: -208 / +90 net.
FB-010 | 2026-06-18 | b7d32c4 | a | Added Sentiment Insights section to the AEO PR Influence tab, between Section B (PR Placement Matchback) and Section C (Top Editorial Domains) per Tina's screenshot placement. STATIC content sandboxed to Avenue Z: SANDBOX_CLIENT_SLUG = 'avenue-z' gate at the top of the component returns null for any other client, preventing the hardcoded Avenue Z URLs from rendering on iPullRank, Shopify, etc. Layout: section header with Sparkles icon + sentiment KPI pill (Positive 89.4%, brand green) in the top-right; two-column grid below for Positive Themes (left, 8 themes from Tina's "Sources of Positive Claims" table with citation count chips) and Weaknesses (right, 2 themes with their explanation paragraphs). Each row is an accordion (ChevronRight that rotates 90deg on expand) with `max-h-[400px] overflow-y-auto` per column to keep the page footprint bounded. URLs render as `hostname.com · /path` linked targets opening in new tabs. Client component (`'use client'`) using two `Set<number>` states for per-side expanded indices. No data layer changes, no Glean call, no fetch. New file: components/report-sections/peec-ai/sentiment-insights.tsx. Modified: components/report-sections/peec-ai/pr-influence.tsx (import + one render between Matchback and Top Editorial Domains).
FB-009 | 2026-06-18 | 23e7a1a | a | First batch on the official-feedback-pr-influence-tab branch. (a) ADD: new <PRInfluenceSynopsis> executive AI-generated synopsis card at the top of the AEO PR Influence tab, mirroring the FB-002c/FB-003 Overview synopsis shell. Glean-backed, cached 1h per (clientSlug, dateRange), strict-JSON output (`{synopsis, actions}`), graceful try/catch fallback. PR-Influence-specific data inputs: AI Visibility/Position deltas, total AI citations, PR placement matchback summary (X of Y cited by AI), GA4 AI referral sessions + delta, editorial coverage totals + brand-absent count, top 5 brand-absent editorial domains with citation counts, top 3 opportunity clusters with scores. Synopsis is model-filter-agnostic by design (same as Overview synopsis). New files: lib/peec/pr-influence-synopsis.ts and components/report-sections/peec-ai/pr-influence-synopsis.tsx. (b) REMOVE: the entire Section A KPI Strip block on PR Influence — the duplicate `<h3>` "How is AI-driven PR coverage performing?" eyebrow (the SectionHeader from FB-001 already renders that question at the top) plus all 6 `<KpiCard>` instances beneath it (AI Visibility %, Avg AI Position, # AI Citations, PR Placements Cited by AI, AI Referral Sessions, Editorial Share Brand Absent). Pruned the now-dead intermediate vars that fed only those cards (`llmFiltered`, `filteredAiVisibilityPct`, `filteredAvgPosition`, `displayAiVisibility`/`Delta`, `displayAvgPosition`/`Delta`, model-filtered `totalCitations`) and the imports they needed (`KpiCard`, `PEEC`, `GA4`, `sumByModel`). Sections B-F (Matchback, Top Editorial Domains, Brand-Absent Editorial Domains, Prompt Cluster Opportunity Matrix, Next Pitch Opportunities, scoring methodology) untouched.
