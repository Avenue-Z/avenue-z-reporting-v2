# Feedback Changelog

Terse one-line entries of what shipped. Lookup table to answer "did we already fix this?"

Format: `FB-NNN | <date> | <commit-sha> | <verification> | <one-line summary>`

Verification codes: `a` = reasoning only, `b` = ran dev server + clicked through, `c` = user-eyeballed.

---

FB-001 | 2026-06-17 | 7097a19 | a | Unified AEO section header across all 4 tabs via shared SectionHeader component (Overview + PR Influence get the treatment, Content Impact unchanged, Technical Performance shifted yellow to green).
FB-002 | 2026-06-17 | ae8fc06 | a | AEO Overview tab redesign: removed what-changed pills (002a), swapped 3 KPI cards to Visibility / Citation Share / AI Referral Traffic with truth-grounded Citation Share math for both Peec and Profound (002b), added Vertex Gemini exec synopsis at top (002c — superseded by FB-003), added Snapshot KPIs eyebrow (002d), reordered trend chart below KPI grid (002e).
FB-003 | 2026-06-17 | e33ed66 | a | Migrated FB-002c synopsis from Vertex Gemini to Glean Chat API (project-wide Glean-only rule). Added gleanChat() helper to lib/glean.ts; surgical swap in lib/peec/synopsis.ts; cache version bumped to v2-glean; zero render-layer changes. lib/bigquery/gemini.ts (Fun Spot) still on Vertex, flagged as a future migration item.
FB-004 | 2026-06-18 | da74c23 | a | Added vertical axis to AEO Overview VisibilityChart. Five tick labels (integer % with % suffix) computed from existing CHART_MAX, aligned to the 5 existing gridlines. X-axis date row gets a matching w-9 spacer to preserve bar/label alignment. One file: components/report-sections/peec-ai/visibility-chart.tsx. Truth-grounded scale (no nice-rounding).
FB-005 | 2026-06-18 | 6142968 | b | Disambiguated "Google" in AEO Model Breakdown — discovered via live Peec API probe that Gemini data was being silently bucketed into the "Google" row (model_channel.id `google-2` = gemini-scraper, did not match "gemini" substring, fell into "google"). Fix: added model_id dimension to /reports/brands and /reports/domains queries; inverted normalizeSource read priority to prefer row.model.id (friendly scraper id like "gemini-scraper") over row.model_channel.id (channel id like "google-2"). Added MODEL_DISPLAY_LABELS map; "Google" canonical id now displays as "Google AI Overview". Verified end-to-end: before = 3 rows with merged Google bucket, after = 4 rows with separate Gemini + Google AI Overview. AI Mode add-on still lumps into Google bucket, flagged as future work.
FB-006 | 2026-06-18 | d9f8f70 | a | Added Biggest Winners and Biggest Losers cards to AEO Overview tab, side by side between Model Breakdown and Leaderboard. STATIC content: Tina's verbatim 17 winners + 20 losers from her AEO Analysis doc (2026-06-16, Profound, Last 14 Days, ChatGPT) live inline in the component as two const arrays. No props, no data layer, no fetch. Symmetrical layout via grid lg:grid-cols-2 + items-stretch; each card body capped at max-h-[400px] overflow-y-auto so the page footprint stays bounded. Initial commit `364f696` was a computed/live version; superseded by `d9f8f70` per Thomas: static for now. Swap to live data later by replacing the two const arrays with props.
FB-007 | 2026-06-18 | 2077037 | a | Removed BrandSOVChart ("Which categories of brands earn AI share of voice?") and BrandDefinitions ("What do these brand categories mean?") from the AEO Overview tab per Tina. Collapsed the surrounding lg:grid-cols-[1fr_280px] wrapper so the Rankings table ("Which brands appear most often in AI answers?") stretches full width. Also deleted the unused BRAND_TYPE_MAP/BRAND_TYPE_COLORS/BRAND_TYPE_DEFINITIONS import. Domain Types section directly below is untouched. One file: components/report-sections/peec-ai/index.tsx.
FB-008 | 2026-06-18 | c19733e | a | Recolored the AEO Overview Domain Types chart ("What kinds of sources do AI models cite?") and matching legend ("What do these domain types mean?") with the Avenue Z brand palette per Tina. Extracted shared DOMAIN_TYPE_COLORS const so chart bar and legend dot always match. Primary categories use all five brand accents at 100%: Own cyan, UGC green, Editorial blue, Corporate yellow, Competitor purple. Secondary categories use parent-accent at 60% opacity (Reference = blue 60%, Institutional = purple 60%). Other uses white 20%. Zero #8A8A8A gray remains in the file. Brand-doc compliant (opacity on accents is the same technique the brand doc uses for focus/borders).
FB-011 | 2026-06-22 | b4906a2 | a | Placement correction for FB-010. Moved <SentimentInsights /> from between Section B (PR Placement Matchback) and Section C (Top Editorial Domains) to between the Executive Synopsis and Section B. Strict re-read of Tina's mockup: her sequence is Synopsis -> Sentiment Insights -> Top Editorial Domains; Matchback wasn't on her sketch so it lives below. One JSX move + inline comment update in components/report-sections/peec-ai/pr-influence.tsx. New page order: SectionHeader -> Synopsis -> Sentiment Insights -> Matchback -> Top Editorial -> Brand-Absent -> Opportunity Matrix -> Next Pitch -> Methodology. Component itself unchanged; same Avenue Z sandbox gate.
FB-017 | 2026-06-22 | 75db637 | a | Label correction on FB-010 Sentiment Insights. Tina's literal layout spec said "Positive Themes & Negative Themes side-by-side" but FB-010 shipped the right column as "Weaknesses" because Tina's underlying CONTENT for that column was framed that way (Unclear Answer Engine Methodology, Unproven Answer Engine Impact — gaps). Renamed to Tina's literal text. Changes: <h4>Weaknesses</h4> → <h4>Negative Themes</h4>; "Tap a weakness to see the explanation." → "Tap a theme to see the explanation."; updated the top-of-file comment. Zero changes to the data array, state, behavior, or Avenue Z sandbox gate. Files: components/report-sections/peec-ai/sentiment-insights.tsx.
FB-016 | 2026-06-22 | 4215718 | a | Visual bug fix on the FB-012 Prompt Clusters bar chart. Tina spotted "the percentage is, like, in black, and you can't see it." Recharts <Tooltip> has 3 separate style props (contentStyle for container, labelStyle for the row label, itemStyle for each data row). I had set color:'#FFFFFF' on contentStyle only, but Recharts' DefaultTooltipContent applies its own near-black color:rgb(51,51,51) default to the item row, which is invisible against the dark #272727 tooltip background. Fix: added labelStyle={{color:'#FFFFFF', fontWeight:600}} + itemStyle={{color:'#FFFFFF'}} to the <RechartsTooltip>, removed redundant contentStyle.color. Tooltip now renders cluster name in bold white + metric line in white. Files: components/report-sections/peec-ai/pr-influence-tables.tsx.
FB-015 | 2026-06-22 | 81b2277 | a | Close FB-014 open risk #1. Remove PR Placement Matchback from the AEO PR Influence tab — Tina's recommended-layout mockups across FB-011, FB-012, and FB-014 (three rounds) consistently omit Matchback, and Thomas's stated layout order (synopsis → sentiment → top editorial → prompt clusters → top editorial opportunities) is 5 sections, no Matchback. Removed: PRPlacementMatchbackTable JSX render in pr-influence.tsx; matchbackTableRows builder block; 4 demo arrays that fed it (DEMO_PROMPT_CLUSTERS, DEMO_AI_ENGINES, DEMO_PROMPT_COUNT, DEMO_POST_PUBLISH_TREND); PRPlacementMatchbackTable + PRPlacementMatchbackRow + AI_ENGINES_TOOLTIP + POST_PUBLISH_TOOLTIP from pr-influence-tables.tsx; PRPlacementMatchbackTable + PRPlacementMatchbackRow imports. KEPT: buildMatchback helper + matchbackRows + filteredMatchbackRows + placementsCitedByAI compute — the executive synopsis context still uses placementsCitedByAI for "X of Y placements cited by AI" prose. Final tab render order: SectionHeader → PRInfluenceSynopsis → SentimentInsights → [TopEditorialDomainsTable + PromptClusterOpportunityMatrix side-by-side in grid lg:grid-cols-2] → BrandAbsentEditorialDomainsTable (Top Editorial Opportunities) → footer. 5 visible sections, structurally 1:1 with Tina's mockup. Universal change. Files: components/report-sections/peec-ai/pr-influence-tables.tsx, components/report-sections/peec-ai/pr-influence.tsx.
FB-014 | 2026-06-22 | 293599c | a | Per Tina's Jun 17 doc batch on the Brand-Absent card + the Next Pitch section. RETITLE the Brand-Absent table as "Top Editorial Opportunities" (new title: "What are the top pitch opportunities for getting our brand mentioned in AI?"; new subtitle: "Prompt-level citations on the rise where your brand is not mentioned, revealing outreach opportunities that may require different strategies depending on the type of article being cited."). COLUMNS revised to 5: Publication, Article (combined title + URL as a hyperlinked cell), Competitors Mentioned, Citation Share, Delta of Citation Share. Removed columns: Brand Mentioned, Opportunity Priority, Suggested PR Angle. FILTER: brand-not-mentioned (or no data, for manual check) AND positive delta on citation share (`d.retrievedDelta > 0`); applied at table-row build step only — synopsis context keeps the unfiltered brand-absent count for executive accuracy. Removed: bottom footnote, `isDemo` prop, BrandAbsentEditorialDomainRow fields (brandMentioned/opportunityPriority/suggestedAngle), added `citationCountDelta` field. DELETED: entire NextPitchOpportunitiesTable component + NextPitchOpportunityRow type from pr-influence-tables.tsx; nextPitchRows + nextPitchEmptyKind compute + Sparkles JSX wrapper from pr-influence.tsx; Sparkles import. (Tina's "Where should we pitch next to close AI visibility gaps?" REMOVE.) The "How is the opportunity score calculated?" REMOVE was already shipped in FB-012. Matchback table KEPT in place — Tina did not include it in her REMOVE list; carrying as the highest-likelihood next-feedback item. Universal change (no Avenue Z sandbox gate needed). Files: components/report-sections/peec-ai/pr-influence-tables.tsx, components/report-sections/peec-ai/pr-influence.tsx. Diff: -254 / +53 net.
FB-013 | 2026-06-22 | 41d2091 | a | Fix pre-existing data bug surfaced by FB-012's bar chart. computeOpportunityRows() was computing editorialCitationDensity ONCE globally (totalEditorialCitations / N) and assigning the same value to every cluster row, so the FB-012 chart rendered identical 100% bars across all clusters with no ranking. Old 7-col table hid this because 6 other columns varied per cluster (Tina's original screenshot also shows Editorial Citation Density at 100.0% for every row). Fix: compute editorialShare PER CLUSTER using data already fetched — coverage.tagNameById (reversed to clusterName -> tagId), coverage.tagIdsByDomain (host -> tag ids), topDomain.retrieved (citation share %). For each cluster: editorialShare = sum(retrieved across editorial-typed domains tagged with cluster) / sum(retrieved across ALL domains tagged with cluster) * 100. computeOpportunityRows() signature now takes (trackedPrompts, editorialDomains, topDomains, coverage). Call site at pr-influence.tsx:362 updated. No render-layer changes, no fetch changes, no demo-mode changes. opportunityScore values shift downstream (affects Next Pitch priority badges only — the score itself is no longer rendered to users post-FB-012). Files: components/report-sections/peec-ai/pr-influence.tsx.
FB-012 | 2026-06-22 | f1d5c5a | a | Reduce + redesign on the AEO PR Influence tab per Tina's Jun 17 batch (+ Whitney "agreed"). TOP EDITORIAL DOMAINS: rename Citation Count -> Citation Share; drop Avg Citations + PR columns; drop bottom-left blue/green legend; swap subtitle to "These domains are the most likely to surface as cited sources in AI-generated results, so they should be prioritized on the media target list."; drop green-on-PR styling (Domain text + bar) since the legend that explained it is gone; remove unused isDemo/prDataAvailable props. PROMPT CLUSTER OPPORTUNITY: entire 7-column SortableTable replaced with a Recharts horizontal BarChart over editorialCitationDensity, descending, fixed 0-100% domain, editorial-blue single color. New subtitle "Topics ranked by share of citations earned from editorial sources. Higher share means a stronger candidate for the next PR pitch." Old "Opportunity Score = 35% editorial..." footer line removed. LAYOUT: both reduced cards now sit side-by-side in a grid gap-6 lg:grid-cols-2 wrapper directly under SentimentInsights; PR Placement Matchback moves below them (matches Tina's "Recommended layout" screenshot and the FB-011 open risk that anticipated this exact reorder). Removed the bottom "How is the opportunity score calculated?" 4-weight methodology block from pr-influence.tsx — it explained an opportunityScore number that no longer renders anywhere visible. Universal change (no Avenue Z sandbox gate needed — pure design / layout / UX). Files: components/report-sections/peec-ai/pr-influence-tables.tsx, components/report-sections/peec-ai/pr-influence.tsx. Diff: -208 / +90 net.
FB-010 | 2026-06-18 | b7d32c4 | a | Added Sentiment Insights section to the AEO PR Influence tab, between Section B (PR Placement Matchback) and Section C (Top Editorial Domains) per Tina's screenshot placement. STATIC content sandboxed to Avenue Z: SANDBOX_CLIENT_SLUG = 'avenue-z' gate at the top of the component returns null for any other client, preventing the hardcoded Avenue Z URLs from rendering on iPullRank, Shopify, etc. Layout: section header with Sparkles icon + sentiment KPI pill (Positive 89.4%, brand green) in the top-right; two-column grid below for Positive Themes (left, 8 themes from Tina's "Sources of Positive Claims" table with citation count chips) and Weaknesses (right, 2 themes with their explanation paragraphs). Each row is an accordion (ChevronRight that rotates 90deg on expand) with `max-h-[400px] overflow-y-auto` per column to keep the page footprint bounded. URLs render as `hostname.com · /path` linked targets opening in new tabs. Client component (`'use client'`) using two `Set<number>` states for per-side expanded indices. No data layer changes, no Glean call, no fetch. New file: components/report-sections/peec-ai/sentiment-insights.tsx. Modified: components/report-sections/peec-ai/pr-influence.tsx (import + one render between Matchback and Top Editorial Domains).
FB-009 | 2026-06-18 | 23e7a1a | a | First batch on the official-feedback-pr-influence-tab branch. (a) ADD: new <PRInfluenceSynopsis> executive AI-generated synopsis card at the top of the AEO PR Influence tab, mirroring the FB-002c/FB-003 Overview synopsis shell. Glean-backed, cached 1h per (clientSlug, dateRange), strict-JSON output (`{synopsis, actions}`), graceful try/catch fallback. PR-Influence-specific data inputs: AI Visibility/Position deltas, total AI citations, PR placement matchback summary (X of Y cited by AI), GA4 AI referral sessions + delta, editorial coverage totals + brand-absent count, top 5 brand-absent editorial domains with citation counts, top 3 opportunity clusters with scores. Synopsis is model-filter-agnostic by design (same as Overview synopsis). New files: lib/peec/pr-influence-synopsis.ts and components/report-sections/peec-ai/pr-influence-synopsis.tsx. (b) REMOVE: the entire Section A KPI Strip block on PR Influence — the duplicate `<h3>` "How is AI-driven PR coverage performing?" eyebrow (the SectionHeader from FB-001 already renders that question at the top) plus all 6 `<KpiCard>` instances beneath it (AI Visibility %, Avg AI Position, # AI Citations, PR Placements Cited by AI, AI Referral Sessions, Editorial Share Brand Absent). Pruned the now-dead intermediate vars that fed only those cards (`llmFiltered`, `filteredAiVisibilityPct`, `filteredAvgPosition`, `displayAiVisibility`/`Delta`, `displayAvgPosition`/`Delta`, model-filtered `totalCitations`) and the imports they needed (`KpiCard`, `PEEC`, `GA4`, `sumByModel`). Sections B-F (Matchback, Top Editorial Domains, Brand-Absent Editorial Domains, Prompt Cluster Opportunity Matrix, Next Pitch Opportunities, scoring methodology) untouched.
